#!/usr/bin/perl
# The line above is known as the shebang line, which indicates the location of the Perl interpreter. 
# This is necessary for Unix-based systems to know that this is a Perl script.

use strict; 
# This is a Perl pragma that forces you to write 'cleaner' code by preventing you from doing certain potentially harmful things.

use warnings;
# This pragma will cause Perl to give you warnings about questionable parts of your code. 

print "Hello, World!\n";
# The 'print' function outputs text. In this case, it's printing the string "Hello, World!" 
# The "\n" is a newline character, which causes a line break.
